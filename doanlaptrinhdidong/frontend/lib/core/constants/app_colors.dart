import 'package:flutter/material.dart';

class AppColors {
  // Màu xanh chính (Header, Buttons, Badges)
  static const Color primaryGreen = Color(0xFF005D4B);

  // Màu xanh lục nhạt (Dùng cho background của Badge/Tag)
  static const Color lightGreen = Color(0xFFE6F0EB);

  // Màu nền màn hình
  static const Color background = Color(0xFFF3F6F4);

  // Màu trắng Card/Thẻ
  static const Color cardSurface = Color(0xFFFFFFFF);

  // Màu vàng Accent (Nút +, Rating)
  static const Color accentYellow = Color(0xFFFFB800);

  // Màu chữ
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
}